import { createUseStyles } from "react-jss"

export default createUseStyles( (theme) => ({
    carlistBtnContainer: {
        marginTop: '2rem', 
        width: '7rem', 
        height: '2.5rem', 
        background: 'green', 
        display: 'flex', 
        alignItems: 'center', 
        justifyContent: 'center', 
        cursor: 'pointer',
        
    }
}))
